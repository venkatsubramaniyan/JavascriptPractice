const browserName="chrome";

function getBrowserVersion() {
    
    if (browserName==="chrome")
    {
        //let browserVersion=20;
        var browserVersion=20;
    }

    console.log(browserVersion);
    
}

getBrowserVersion();